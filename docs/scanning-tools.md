# Scanning, Linting & Security Tooling — Current State

This is a **current-state reference**, not a history — it says what's true right now. For _why_ each tool was chosen (alternatives considered, tradeoffs, cost), see [ADR-00009](./adrs/00009-linting-formatting-and-security-scanning.md) (the original matrix) and [ADR-00010](./adrs/00010-github-actions-supply-chain-hardening.md) (GitHub Actions pinning + `zizmor`). The ADRs don't get rewritten as tools are added or reconfigured; this file does.

**If you add, remove, or reconfigure a scanning/linting tool, update this table in the same commit.** That's the whole point of it existing — see the convention in `AGENTS.md`.

| Tool                                                         | Category                       | Checks for                                                                                                                                                                                                                                          | Runs                                                                                                                | Enforcement                                     | Config                                      |
| ------------------------------------------------------------ | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------- |
| ESLint (core)                                                | Code quality                   | Real bugs + consistent patterns, AST-based                                                                                                                                                                                                          | Pre-commit (staged) · PR CI (`nx affected -t lint`)                                                                 | Blocking                                        | `eslint.config.mjs` (+ per-app overrides)   |
| typescript-eslint                                            | Code quality                   | TypeScript-aware lint rules (syntactic tier; type-aware tier off)                                                                                                                                                                                   | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-sonarjs                                        | Code quality                   | Cognitive complexity, duplicate branches, code smells                                                                                                                                                                                               | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-jsx-a11y                                       | Code quality                   | Accessibility (alt text, ARIA, keyboard nav)                                                                                                                                                                                                        | Bundled into `apps/web`'s ESLint pass                                                                               | Blocking                                        | `apps/web/eslint.config.mjs`                |
| eslint-plugin-jest (`no-focused-tests`, `no-disabled-tests`) | Code quality                   | Accidentally committed `.only`/`.skip` silently dropping test coverage from CI (ADR-00007)                                                                                                                                                          | Scoped to `**/*.spec.ts`/`**/*.test.ts`, excluding `apps/web-e2e` (Playwright's own equivalent already covers that) | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-react-hooks                                    | Code quality                   | Rules of Hooks, exhaustive-deps                                                                                                                                                                                                                     | Bundled into `apps/web`'s ESLint pass                                                                               | Blocking                                        | `apps/web/eslint.config.mjs`                |
| `@next/eslint-plugin-next` (`core-web-vitals`)               | Code quality                   | Next.js-specific correctness (image usage, script loading, Core Web Vitals) — was imported but never actually applied until now                                                                                                                     | Bundled into `apps/web`'s ESLint pass                                                                               | Blocking                                        | `apps/web/eslint.config.mjs`                |
| Prettier                                                     | Formatting                     | Deterministic code formatting                                                                                                                                                                                                                       | Pre-commit (staged — autofix + re-stage) · PR CI (`format:check`)                                                   | Autofix (pre-commit) / Blocking (PR CI)         | `.prettierrc` / `prettier.config.*`         |
| prettier-plugin-tailwindcss                                  | Formatting                     | Tailwind class-name sort order                                                                                                                                                                                                                      | Bundled into the Prettier pass above                                                                                | Autofix / Blocking (same as Prettier)           | Prettier config                             |
| TypeScript compiler (`tsc`)                                  | Type checking                  | Full type inference, not just lint-level checks                                                                                                                                                                                                     | Pre-push (`nx affected -t typecheck --base=main`) · PR CI (always)                                                  | Blocking                                        | `tsconfig.json` (root + per-project)        |
| `@nx/enforce-module-boundaries`                              | Architecture boundaries        | Tag-based dependency rules — `type:lib`/`type:app` may only depend on `type:lib` (never on a `type:app`, including each other) (ADR-00004: apps are thin, packages hold the logic; ADR-00003: no app's internals are another app's public contract) | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-import-x                                       | Architecture boundaries        | Import ordering, circular deps (`no-cycle`), unresolved imports                                                                                                                                                                                     | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-n (`no-process-env`)                           | Architecture boundaries        | Direct `process.env` access outside the per-package `createEnvGetter` pattern (ADR-00013)                                                                                                                                                           | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| `no-restricted-syntax` (relative `.js` import ban)           | Architecture boundaries        | Relative imports ending in `.js` (nodenext-style, resolves to a real `.ts` file) — Next.js's bundler can't resolve this when consuming a workspace package as source (ADR-00011)                                                                    | Bundled into the ESLint pass above, scoped to `**/*.ts`/`**/*.tsx`                                                  | Blocking                                        | `eslint.config.mjs`                         |
| eslint-plugin-security                                       | Security (SAST)                | AST-level security smells (eval, unsafe regex, child_process)                                                                                                                                                                                       | Bundled into the ESLint pass above                                                                                  | Blocking                                        | `eslint.config.mjs`                         |
| Semgrep                                                      | Security (SAST)                | Broader pattern-based SAST (community rulesets)                                                                                                                                                                                                     | CI `security-warn` job · `pnpm run health` (fuller ruleset sweep)                                                   | Warn-only                                       | Inline `--config` flags (no dedicated file) |
| Bearer                                                       | Security (SAST)                | Data-flow / PII / sensitive-data tracing                                                                                                                                                                                                            | CI `security-warn` job · `pnpm run health` (fuller sweep)                                                           | Warn-only                                       | None (defaults)                             |
| Gitleaks                                                     | Secrets                        | Regex-pattern secret detection, diffs and history                                                                                                                                                                                                   | Pre-commit (staged) · PR CI (`main` job) · `pnpm run health` (full-history sweep)                                   | Blocking (pre-commit, PR CI) / Warn (health)    | None (default ruleset)                      |
| `pnpm audit`                                                 | Dependency vulnerabilities     | Installed versions vs. the npm advisory database                                                                                                                                                                                                    | `pnpm run health` only                                                                                              | Warn-only                                       | None                                        |
| osv-scanner                                                  | Dependency vulnerabilities     | Broader OSV/GHSA vulnerability database                                                                                                                                                                                                             | `pnpm run health` · dedicated weekly `osv-scan` CI job (uploads SARIF to the Security tab)                          | Warn-only (health) / informational (SARIF)      | None (CLI args only)                        |
| Renovate                                                     | Dependency updates             | Opens PRs for outdated/vulnerable dependencies                                                                                                                                                                                                      | Own schedule (+ Monday lockfile maintenance)                                                                        | N/A — opens PRs, doesn't gate anything directly | `renovate.json`                             |
| Syncpack                                                     | Dependency version consistency | Cross-package version mismatches for the same dependency (duplicate runtime instances, structural type incompatibilities) — ADR-00004                                                                                                               | PR CI (`pnpm run deps:check`)                                                                                       | Blocking                                        | `pnpm-workspace.yaml` (`catalog:`)          |
| Knip                                                         | Dead code / unused deps        | Unused files, exports, dependencies (monorepo-aware)                                                                                                                                                                                                | `pnpm run health` · on-demand locally                                                                               | Warn-only                                       | `knip.json`                                 |
| `zizmor`                                                     | Actions supply chain           | Workflow-file security: unpinned action refs, credential handling, etc.                                                                                                                                                                             | Pre-push (soft — skips if not installed) · PR CI `zizmor` job                                                       | Blocking (PR CI) / soft check (pre-push)        | None yet (defaults)                         |

## Known gaps (tracked, not silent)

- **`@nx/enforce-module-boundaries` only enforces "no `type:app` on either side of the dependency"** — neither packages nor apps may depend on an app (ADR-00003, ADR-00004). Finer-grained layering between individual packages (e.g. "domain can't import db directly") is deliberately not enforced yet — those packages don't exist, and guessing at that boundary before there's real code to test it against would be premature.
- **`ci.yml`'s `main`/`security-warn` jobs don't `needs: zizmor`** — within one CI run, a reverted action pin could still execute in those jobs before the `zizmor` job fails it. Job-dependency + branch protection is deferred to a later pass (see `AGENTS.md`).
- **Semgrep/Bearer are warn-only by design for now** — promoting either to blocking is deliberate future work once their false-positive rate is proven low on this codebase, not automatic.
- **Nx prints `Dependency "zod" could not be resolved for catalog "default"`** during `lint`/`build`/`test`/`typecheck` — Nx's own project-graph analysis doesn't yet fully understand pnpm's `catalog:` protocol. Confirmed harmless: pnpm itself resolves it correctly (`pnpm install`, the actual build, and `syncpack lint` all work), and every task still passes. Revisit if Nx ships proper catalog support and the noise is worth removing.
