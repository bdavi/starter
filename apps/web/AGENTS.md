# AGENTS.md — apps/web

See [README.md](./README.md) for what this app is, its stack, and its commands — not repeated here.

## Agent-specific notes

- Tagged `type:app` for Nx's module-boundary rule (`@nx/enforce-module-boundaries` in the root `eslint.config.mjs`). That rule is currently permissive (only one project exists) — don't assume it's enforcing anything yet.
- `next-env.d.ts` is auto-generated by Next.js ("This file should not be edited") and is excluded from `import-x/no-unresolved` in the root ESLint config — it references a path inside `.next/`, gitignored build output that doesn't exist on a fresh checkout before a build has run. Don't remove that exclusion without understanding why it's there (see the commit that added it).
- `src/app/**` files (pages, layouts, routes) are discovered by Next.js's App Router file-based routing, not imported explicitly anywhere. Don't assume a file is dead code just because nothing references it — check whether it follows the routing convention first (Knip's config already accounts for this).
- No real business logic exists yet — don't build on assumptions about what this app "does" beyond what's in the code right now.
